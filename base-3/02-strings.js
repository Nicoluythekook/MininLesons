const firstnName = 'Niko'
const age = 25

function getAge() {
    return age
}
//const output = `Hello my name is ${name} and my age is ${getAge() > 18 ? 'Can drive' : 'Cant drive'}`
//console.log(output)
console.log (firstnName.length)
console.log (firstnName.toUpperCase())
console.log (firstnName.toLowerCase())
console.log (firstnName.charAt(2))
console.log (firstnName.indexOf('!'))  //-1
console.log (firstnName.toUpperCase().startsWith('Ni'))
console.log (firstnName.endsWith('o'))
console.log (firstnName.repeat(100))

const password = '     my super pas        '

console.log(password.trim())

